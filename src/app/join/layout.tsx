import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Sign Up | GetA2PApproved",
  description:
    "Create your GetA2PApproved account. Generate carrier-compliant Privacy Policies, Terms & Conditions, and Submission Language for A2P 10DLC registration in minutes.",
};

export default function JoinLayout({ children }: { children: React.ReactNode }) {
  return children;
}
